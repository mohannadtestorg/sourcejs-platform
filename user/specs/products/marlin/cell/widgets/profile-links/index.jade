extends /specs/products/marlin/cell/page-structure/structure/templates/structure/layout-tmpl.jade

block content
    section.source_section
        h2 Example
        p The following example demonstrates profile links widget.

        if (data.items)
            each item , i in data.items
                h3!= item.title
                .source_example
                    .ux3
                        .container.dark-region
                            | !{item.body}
    | #{''}