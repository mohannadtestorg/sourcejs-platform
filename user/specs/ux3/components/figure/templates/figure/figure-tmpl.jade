

each item, l in list
    figure.figure.article__tabFigure(id=item.figureId)
        img.figure__image(src=item.src)
        figcaption
            span.figure__title !{item.title}
            span.figure__caption !{item.caption}
