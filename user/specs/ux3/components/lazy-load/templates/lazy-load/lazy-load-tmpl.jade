mixin loading(image)
    .lazy-load
        if (image == 'true')
            .lazy-load__img
        .lazy-load__text
            .lazy-load__line
            .lazy-load__line--80
            .lazy-load__line
            .lazy-load__line--70
            .lazy-load__line--50